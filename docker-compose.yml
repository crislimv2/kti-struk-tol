services:
  web:
    build: .
    image: kti-struk-tol:latest
    container_name: kti-struk-tol
    restart: unless-stopped
    ports:
      - "3000:3000"
    environment:
      - NODE_ENV=production
      # Opsional (login + riwayat cloud). Tanpa ini app tetap jalan.
      # - NEXT_PUBLIC_SUPABASE_URL=
      # - NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY=
